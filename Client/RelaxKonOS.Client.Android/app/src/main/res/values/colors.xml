<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Mirrors the top stop of the light RelaxKonOS backdrop gradient
      (ui/theme/Palette.kt, RelaxKonLightColors.backdropStart) so the window frame drawn before the
      first Compose frame does not flash a different colour than the app paints over it.
    -->
    <color name="window_background">#F7F9FE</color>
</resources>

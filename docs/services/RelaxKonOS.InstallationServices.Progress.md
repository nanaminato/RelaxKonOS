# 受管安装服务实施进度

对应 [Goal](./RelaxKonOS.InstallationServices.Goal.md)，更新日期：2026-09-11。

状态：实施中。遵照本轮要求，优先功能实现，测试编写和执行后置；编译检查单独记录，不视为测试验收。

## 实施记录

- 已添加统一 Protocol：冻结枚举、阶段内可空进度、服务专属严格输入 DTO、统一路由和稳定 problem code。
- 已添加宿主唯一安装账本：原子替换、落盘 flush、请求者/幂等/输入/锁资源不可逆引用，状态和审计同一次提交，损坏后 fail closed。
- 已添加 Coordinator：提交脱离 HTTP 生命周期、提交时资源冲突检测、身份范围内幂等、取消安全边界、启动只读恢复、关闭中断处理。
- 待完成：领域适配器、授权/Endpoint、所有 Client 调用迁移、旧协议删除、Helper 阶段流、平台安装实现与文档同步。

## 本轮明确跳过的测试（待补）

- Protocol 序列化、非法枚举/百分比/未知选项拒绝。
- 账本损坏、原子替换失败、身份隔离、幂等键复用与不同参数冲突。
- 多领域资源锁、取消与不可取消阶段竞态、HTTP 断开、Server 启动/关闭恢复。
- 各领域安装、升级、修复、卸载后的真实检测、失败回滚和恢复判定。
- Helper NDJSON 帧大小/数量/版本/阶段验证，APT Status-Fd 和 Windows role 阶段。
- Client 轮询退避、断线重连、窗口重开、工作区持久化与本地化。
- 隔离 Ubuntu / Windows Server VM 验收：缓存、无 Content-Length、慢网、校验失败、APT/dpkg 冲突、健康失败及支持矩阵。

## 编译检查

- 尚未执行；待公共模块和首批领域接入后检查。

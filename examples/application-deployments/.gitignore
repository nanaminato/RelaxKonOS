# 夹具构建过程中的临时目录。
#
# 每个 build.sh 在成功结束时都会自行删掉它们，但一次失败或中断的构建会把它们留在磁盘上，
# 那时 git 就会把它们当成新增文件拉进提交。bin/ 与 obj/ 由仓库根的 .gitignore 覆盖，
# 这里补上构建脚本自己创建的那几个。
#
# 注意 dist/ 不在此列：归档本身就是测试输入（上传单元），必须进版本库。
build/
stage/
.publish/
__pycache__/
